SPECIFICATION Spec
CONSTANTS
  Calls = {c1, c2}
  MODE = "asWritten"
INVARIANT TypeOK
\* NoWrongTeardown falls under this design too; sipwt_asWritten pins that one. A row
\* lists one falling property so TLC's search order cannot change which it reports.
INVARIANT NoBlockedWithPendingControl
CHECK_DEADLOCK FALSE
