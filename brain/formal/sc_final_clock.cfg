\* PR #49 as opened: a final is placed by CLOCK -- the commit's time against the
\* strategy's read of the VAD start, the done's arrival when nothing asked -- on the
\* wire as it is. Kept for the counterexamples the review found (endpointing.py's
\* first cut of the earlier-final rule).
SPECIFICATION Spec
CONSTANTS
    MODE = "verdict"
    MaxStops = 2
    MaxLost = 1
    MaxMissed = 0
    TURNSTOP = "verdict"
    DESIGN = "clock"
    DONES = "unmarked"
    MaxCloses = 1
    MaxBackstops = 1
INVARIANTS
    TypeOK
    NoStaleTurnEnd
